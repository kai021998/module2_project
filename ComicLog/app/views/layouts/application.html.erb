<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>ComicLog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if logged_in? %>
      <h6>Logged in as: <%= current_user.name %></h6>
    <% else %>
      <%= button_to "Log In", new_session_path, method: :get %>
      <%= button_to "Sign Up", new_user_path, method: :get %>
    <% end %>

    <center><img src="https://i.imgur.com/nCbGsyu.png" style="max-width: 100%; min-height: 50px;"></center>
    <div id="outer">
        <% if logged_in? %>
          <div class="inner"><%= button_to "Profile", user_path(current_user), method: :get %></div>
        <% end %>
        <div class="inner"><%= button_to "Users", users_path, method: :get %></div>
        <div class="inner"><%= button_to "Comics", comics_path, method: :get %></div>
        <div class="inner"><%= button_to "Publishers", publishers_path, method: :get %></div>
        <% if logged_in? %>
          <div class="inner"><%= button_to "Log Out", logout_path, method: :delete %></div>
        <% else %>
          <div class="inner"><%= button_to "Log In", publishers_path, method: :get %></div>
        <% end %>
    </div>
<hr>
    <%= yield %>
  </body>
</html>
